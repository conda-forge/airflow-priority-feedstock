{% set name = "airflow-priority" %}
{% set version = "1.5.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/a/airflow-priority/airflow_priority-{{ version }}.tar.gz
  sha256: 66e8ae7baa4017cd6815846fc9019b843080493b13223c709c65aba0406409c3

build:
  number: 0
  script:
    - {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  noarch: python

requirements:
  host:
    - python {{ python_min }}
    - pip
    - hatchling
  run:
    - python >={{ python_min }}
    - apache-airflow >=2,<3
    - pydantic >=2
  run_constrained:
    - airflow-config >=0.1.2

test:
  requires:
    - python ={{ python_min }}
    - pip
  commands:
    - pip check
  imports:
    - airflow_priority.common

about:
  home: https://github.com/airflow-laminar/airflow-priority
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: Priority Tags for Airflow Dags
  description: Priority Tags for Airflow Dags
  dev_url: https://github.com/airflow-laminar/airflow-priority

extra:
  recipe-maintainers:
    - timkpaine
